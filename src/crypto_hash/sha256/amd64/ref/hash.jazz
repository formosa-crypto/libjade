require "sha256.jinc"

export fn jade_hash_sha256_amd64_ref(#transient reg u64 out in inlen) -> reg u64
{
  reg u64 r;
  #msf reg u64 ms;

  ms = #init_msf();
  ms = __sha256(out, in, inlen, ms);
  _,_,_,_,_,r = #set0();
  ms = #init_msf();

  return r;
}
