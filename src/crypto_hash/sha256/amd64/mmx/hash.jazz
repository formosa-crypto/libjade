require "sha256.jinc"

export fn jade_hash_sha256_amd64_mmx(#transient reg u64 out in inlen) -> reg u64
{
  reg u64 r;
  #msf reg u64 ms;

  ms = #init_msf();
  __sha256(out, in, inlen);
  _,_,_,_,_,r = #set0();
  ms = #init_msf();

  return r;
}
