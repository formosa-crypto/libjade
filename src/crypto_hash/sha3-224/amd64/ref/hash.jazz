require "sha3-224.jinc"

export fn jade_hash_sha3_224_amd64_ref(#transient reg u64 out in inlen) -> reg u64
{
  reg u64 r;
  #msf reg u64 ms;

  ms = #init_msf();
  ms = __sha3_224_ref(out, in, inlen, ms);
  _,_,_,_,_,r = #set0();

  return r;
}
