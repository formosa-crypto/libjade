require "shake128.jinc"

export fn jade_xof_shake128_amd64_ref(#transient reg u64 out outlen in inlen) -> reg u64
{
  reg u64 r;
  #msf reg u64 ms;

  ms = #init_msf();
  _ = __shake128_ref(out, outlen, in, inlen, ms);
  _,_,_,_,_,r = #set0();

  return r;
}
