TOP := $(dir $(abspath $(lastword $(MAKEFILE_LIST))))
JFLAGS := ${JFLAGS} -noinsertarraycopy
JASMINC ?= jasminc
JASMIND ?= jasminc --print-dependencies

%.s: %.jazz $(shell $(JASMIND) %.jazz)
	$(JASMINC) $(JFLAGS) -o $@ $<

clean:
	@rm -f *.s *.o *.a
