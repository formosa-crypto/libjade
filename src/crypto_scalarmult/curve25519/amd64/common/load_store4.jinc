inline fn __load4(reg u64 p) -> reg u64[4]
{
  inline int i;
  reg u64[4] a;

  for i=0 to 4
  { a[i] = [p + 8*i]; }

  return a;
}

inline fn __store4(reg u64 p, reg u64[4] a)
{
  inline int i;

  for i=0 to 4
  { [p + 8*i] = a[i]; }
}

