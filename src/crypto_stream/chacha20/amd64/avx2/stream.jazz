require "chacha20.jinc"

export fn crypto_stream_chacha20_amd64_avx2(reg u64 output plain, reg u32 len, reg u64 key nonce, reg u32 counter)
{
  __chacha20_avx2(output, plain, len, key, nonce, counter);
}

