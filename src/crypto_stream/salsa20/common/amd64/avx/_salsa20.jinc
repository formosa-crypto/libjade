inline fn __salsa20_avx(reg u64 output plain len nonce key)
{
  __salsa20_v_avx(output, plain, len, nonce, key);
}

inline fn __salsa20_1_avx(reg u64 output plain len nonce, reg u32[8] key)
{
  __salsa20_v_1_avx(output, plain, len, nonce, key);
}
